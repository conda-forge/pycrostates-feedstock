{% set name = "pycrostates" %}
{% set version = "0.2.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/pycrostates-{{ version }}.tar.gz
  sha256: fa1220365e0efe6a1cc738a332b3f65a7da69ed7833a00b76f0149f991d70263

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 1

requirements:
  host:
    - pip
    - python >=3.8
    - setuptools >=61.0.0
    - wheel
  run:
    - decorator
    - importlib-resources  # Normally only needed for py<39, but we're going noarch here!
    - jinja2
    - joblib
    - matplotlib-base
    - mne-base >=1.0
    - numpy >=1.21
    - pooch
    - python >=3.8
    - scikit-learn
    - scipy

test:
  imports:
    - pycrostates
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://pycrostates.readthedocs.io/en/latest/
  summary: A simple open source Python package for EEG microstate segmentation.
  license: BSD-3-Clause
  license_family: BSD
  license_file: LICENSE
  dev_url: https://github.com/vferat/pycrostates

extra:
  recipe-maintainers:
    - vferat
    - mscheltienne
